## 1. Configurar Docker Compose

- [x] 1.1 Inserir uma definição nova `pgadmin:` no arquivo `docker-compose.yml`, referenciando corretamente as redes `depends_on`, expor a porta e plugar as variáveis de root admin.

## 2. Iniciar Sistema

- [x] 2.1 Rodar `sudo docker compose up -d` na raiz da pasta.
- [x] 2.2 Verificar o status listado no `docker ps`.
